#include <iostream>
#include <map>
using namespace std;

#include "bits.hpp"
using namespace BitMath;

int main()
{
	typedef map<std::string, Int> symbols;
	while (true) {
		string line;
		getline(cin, line);
		istringstream iss(line);
		
		string arr[10];
		
		while (!iss.eof()) {
			string str;
			iss >> str;
			
	}
	return 0;
}
